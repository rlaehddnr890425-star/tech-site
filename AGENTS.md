# 테크한잔 (1p쓰리) 사이트 규칙 (모든 에이전트/세션/크론 필수 준수)

이 폴더(site/)에 쓰는 모든 작업 주체는 아래를 먼저 지킨다.

## 정체
- 라인명: **1p쓰리** = AI·테크 재가공 정보소 「테크한잔」. 자금레이더(1p원), 척척상식(1p투)과 상호링크 금지.
- 컨셉: X(구 트위터)·HackerNews 등 해외에서 조회수·저장수로 검증된 AI/테크 글을 **소재로만** 사용해 한국어 재가공 원고를 만든다.

## 절대 규칙 — 복붙 금지 (2026-09-19 사용자 확정)
- 해외 원문의 문장·표현을 그대로 옮기지 않는다. 논점·구조·수치만 재료로 쓰고 전체를 다시 쓴다 (애드센스 duplicate content 리스크 차단이 목적).
- 수치·링크는 원문과 1차 자료에서 직접 확인한 값만. 확인 실패 시 그 서술을 버린다.
- 조문·출처·검증시각·작성자 메타 표기 금지는 자금레이더 규칙과 동일.

## 개인정보
- 공개 저장소다. 운영자 실명·자격·연락처(lead.n.uk@gmail.com 외) 노출 금지. 운영자 표기는 `테크한잔 (1인 운영 민간 서비스)`.

## 발행 = 3점 연결 (자금레이더와 동일 규칙)
① `posts/<slug>.html` (사이트 템플릿: hwrap 헤더·canonical·style.css 링크) ② 홈 `index.html` `#today` 그리에드 카드 추가 ③ `sitemap.xml` 엔트리 — 셋 다 같은 커밋에.

## 디자인 토큰 (변경 시 style.css :root 만 고친다)
다크 네온: `--void #07090f` 배경 / `--neon #39d0ff` 시안 / `--amber #ffb45c` 위스키 / `--violet #a78bfa` / `--rose #ff7ab8`. 히어로는 `linear-gradient` 그라데이션 텍스트+glow. 로고: 🥃 + gradient 텍스트.

## 게재 전
사용자 검수본 승인 후 push (always-review-before-publish). live 반영은 `verify_deploy.py --repo sangsik` 식의 재사용 스크립트 또는 파일 해시 비교 1회, sleep 폴링 금지.
